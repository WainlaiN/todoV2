<div class="container-fluid shadow-sm">
    <nav class="navbar navbar-expand-lg navbar-light">

        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="navbar-brand" href="{{ path('homepage') }}">
                    <img class="logo" src="{{ asset('img/logoV3.jpg') }}" alt="OpenClassrooms"/>
                    Todo List
                </a>
            </li>
        </ul>
        <button class="navbar-toggler collapsed" type="button" data-toggle="collapse"
                data-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto">

                {% if not app.user and 'login' != app.request.attributes.get('_route') %}
                    <li class="nav-item">
                        <a href="{{ path('app_login') }}" class="nav-link"><i class="fas fa-sign-in-alt"></i>
                            Connexion</a>
                    </li>

                {% endif %}
                {% if  app.user %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <li class="nav-item">
                            <a href="{{ path('user_create') }}" class="nav-link"><i class="far fa-user"></i> Créer
                                un utilisateur</a>
                        </li>
                        <li class="nav-item">
                            <a href="{{ path('user_list') }}" class="nav-link"><i class="fas fa-users-cog"></i>
                                Admin</a>
                        </li>
                    {% endif %}
                    <li class="nav-item">
                        <a href="{{ path('app_logout') }}" class="nav-link"><i class="fa fa-sign-out-alt"></i>
                            Deconnexion</a>
                    </li>
                {% endif %}

            </ul>
        </div>
    </nav>
</div>